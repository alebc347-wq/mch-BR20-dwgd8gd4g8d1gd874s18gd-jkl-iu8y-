# 🤖 倉鼠勇者 2.0 (Hero Bot) 完整指令與功能教學指南

歡迎使用 **倉鼠勇者 2.0**！這是一份詳盡的說明書，為您完整介紹機器人的所有指令用法、隱藏版自動化功能，以及如何解鎖 **Pro 專業版** 與 **Ultra 旗艦版** 會員。

---

## 💎 如何獲取 Pro 專業版與 Ultra 旗艦版

機器人提供兩種進階訂閱等級，能極大化您的伺服器社群與娛樂體驗。

### 1. 💎 Pro 專業版 (Pro Tier)
* **解鎖福利**：
  * **24/7 音樂不斷線**：機器人能永久駐留在語音頻道中，隨時待命。
  * **解鎖大型歌單**：支援解析超過 5 首歌曲的 YouTube 或 Spotify 播放清單。
  * **優先音訊流解碼**：享有更流暢、低延遲的播放體驗。
* **獲取方式**：
  1. 點選推廣訊息中的「獲取 Pro 金鑰」按鈕，或前往官方訂閱網頁：[Pro 金鑰訂閱網站](https://mch-mb20.base44.app/)。
  2. 訂閱成功後即可獲得專屬 Pro 啟用碼（格式例如：`HERO-PRO-XXXX`）。
  3. 當您使用 Pro 專屬功能時，點選彈出畫面中的 **「輸入激活碼」** 即可完成解鎖。

### 2. 👑 Ultra 旗艦版 (Ultra Tier)
* **解鎖福利**：
  * 解鎖所有音樂**進階音效濾波器**（重低音 Bassboost、Nightcore 速度、8D 環繞音效等）。
  * 享有最頂級的伺服器 CPU 運算資源與獨立音訊解碼線路。
  * 支援**「無限制歌曲長度」**與**「無限制佇列」**播放。
  * 解鎖 **「跨群聊天 (Cross-Guild Chat)」** 配對連線，與其他伺服器進行多向即時對話！
* **獲取方式**：
  * **免費訂閱解鎖 (30天)**：**完全免費！** 執行 `/ultra` 指令，點選 **「開始驗證訂閱」**。前往訂閱機器人擁有者的 YouTube 頻道，然後返回 Discord 點選 **「確認我已訂閱」**，即可立即免費使用 Ultra 30 天！
  * **金鑰啟用**：使用透過贊助或活動獲得的 Ultra 金鑰，於 `/ultra` 指令面板中輸入即可快速啟用。

---

## 🛜 核心功能一：跨群聊天配對系統 (Ultra 專屬)

您可以將您的伺服器與多個不同的 Ultra 伺服器進行對接，實現跨群共用頻道的奇妙體驗！

* **`/部署跨群聊天`** (管理員限制)：
  * 在伺服器中建立一個 `🛜-跨群聊天` 頻道，並附帶一個專屬配對碼（例如 `CG-XXXXXX`）。該指令會自動隱藏，並替換為移除指令。
* **`/移除跨群聊天`** (管理員限制)：
  * 取消部署、清除資料庫連線關係，並自動刪除本地的跨群聊天頻道，通知已配對的對方伺服器。
* **配對碼智慧 Modal 控制面板**：
  * 點選頻道中的 **「輸入配對碼」** 按鈕，會彈出可預填的 5 個槽位表單，自動為您預填已連線伺服器的名稱與代碼（例如：`配對碼 1 - CO歡樂群`）。
  * **新增連線**：直接在空白的輸入欄填入新伺服器配對碼。
  * **斷開連線**：將該伺服器的輸入欄清空（Backspace/Delete）並點選提交，或者點擊「斷開連線」按鈕以使用下拉選單快速移除單一或全部連線。

---

## 🎵 核心功能二：多功能音樂系統

強大的串流與播放控制，讓您在 Discord 中享受最完美的音質。

* **`/music_menu`**：召喚音樂系統 UI 控制面板（包含播放、暫停、跳過、音量調節按鈕）。
* **`/play [URL 或關鍵字]`**：搜尋並在您當前的語音頻道中播放音樂。
* **`/pause`**：暫停或恢復播放。
* **`/skip`**：跳過目前播放的歌曲。
* **`/stop`**：停止播放並清空目前的歌曲佇列。
* **`/nowplaying`**：顯示當前正在播放的曲目詳細資料（含歌手、進度條、長度）。
* **`/queue`**：列出目前佇列中的所有歌曲。
* **`/volume [0-100]`**：精準調整播放音量。
* **`/shuffle`**：隨機打亂目前佇列中的歌曲順序。
* **`/loop [單曲/佇列/關閉]`**：設定重複播放模式。
* **`/seek [秒數]`**：跳轉到歌曲的特定時間點。
* **`/247`** (Pro/Ultra 專屬)：開啟後，機器人在沒有播放音樂時也會永久留在語音頻道中。
* **`/filter [濾波器類型]`** (Ultra 專屬)：套用進階音效，如 `bassboost`、`nightcore`、`8d` 等。

---

## 🫨 核心功能三：即時台灣地震與天災監控

直接介接台灣中央氣象署 (CWA) 官方 OpenData，在發生災害時發送第一手通知。

* **`!天災 開啟`** (管理員限制，前置為 Prefix `!`):
  * 自動在伺服器中建立唯讀的 `🫨-地震通知` 頻道，開啟即時地震與豪大雨特報監控推播（每 30 秒自動檢測一次）。
* **`!天災 關閉`** (管理員限制):
  * 關閉此伺服器的天災推播。
* **`!測試天災`**:
  * 測試與氣象署 API 的連線，並在地震通知頻道中發送測試 Embed 卡片。

---

## 🛡️ 核心功能四：自動審核防護系統 (Auto-Mod)

自動在背景默默守護您的伺服器，防止惡意刷屏與不良言論。

* **髒話與敏感詞過濾**：自動檢測非管理員發送的敏感訊息，並立即將其撤回。
* **刷屏防護**：短時間內快速發送大量重複訊息時，會自動進行警告並封鎖該訊息。
* **網址過濾**：在未允許發送網址的頻道中，自動撤回所有網址連結。
* **大寫字母過量偵測**：防止成員在頻道中發送過度洗版的大寫字元。
* **Mention 提及防護**：限制單則訊息中提及他人的人數，防止騷擾。
* **設定與管理指令**：
  * **`/setup`**：手動同步並建立日誌頻道 `顯示伺服器目前變動`，記錄所有警告與刪除事件。
  * **`/set [日誌頻道]`**：自訂日誌訊息的發送位置。
  * **`/config`**：配置要啟用或關閉的日誌記錄事件種類。
  * **`/settings`**：開啟日誌自定義選單，一鍵設定警報機制。

---

## 💬 核心功能五：自訂自動回覆庫 (Auto-Reply)

* **`/add_reply [關鍵字] [回覆內容]`** (管理員限制)：
  * 當有成員提及 `關鍵字` 時，機器人會自動回覆設定的內容（支援變數如 `{user}` 提及發言人）。
* **`/remove_reply`** 或 **`/delete_reply [關鍵字]`**：
  * 刪除自訂的關鍵字回覆。
* **`/toggle_default_replies [開啟/關閉]`**：
  * 設定是否啟用機器人內建的趣味常用詞典（如自動答覆「哈囉」、「掰掰」、「我餓了」、「笑死」等上百個生活化對話）。
* **`/list_replies`**：
  * 查看目前伺服器中所有已設定的自訂自動回覆清單。

---

## 🔨 社群管理與包廂服務 (Moderation & Room)

* **`/kick [成員] [原因]`** / **`!k [成員] [原因]`**：踢出指定成員。
* **`/ban [成員] [天數] [原因]`** / **`!b [成員] [天數] [原因]`**：封禁成員（天數填 0 代表解除封鎖）。
* **`/timeout [成員] [時間] [原因]`** / **`!t [成員] [時間] [原因]`**：禁言成員。
* **`/warn [成員] [原因]`**：警告成員。
* **`/warnings [成員]`**：查看特定成員的警告歷史紀錄。
* **`/clearwarning [成員]`**：清除該成員的所有警告累計。
* **`/warnconfig [臨界警告數] [自動處罰方式]`**：設定成員被警告幾次後自動執行禁言、踢出或封禁。
* **`/purge [訊息數量]`**：批量清理並刪除頻道內指定數量的歷史訊息。
* **`/slowmode [秒數]`**：設定當前頻道的發言冷卻時間。
* **`/lock`** 與 **`/unlock`**：鎖定或解鎖當前頻道的發言權限。
* **`/rename_user [成員] [新暱稱]`**：幫成員修改伺服器暱稱。
* **`/dm [成員] [訊息內容]`**：以機器人的名義私訊指定成員。
* **`/set_role_nick [身分組] [暱稱]`**：一鍵將特定身分組的成員暱稱修改為指定文字（限教皇身份）。
* **`/create_room [語音/舞台] [包廂名稱]`**：
  * 在《包間服務》分類下建立一個專屬的包廂頻道。
  * **動態包房小功能**：這是一個動態語音頻道，當使用者進入後會自動產生子房，當所有人離開後會自動刪除，維持伺服器頻道的整潔！

---

## 🎰 娛樂與遊戲指令

在聊天頻道中與機器人進行各種互動遊戲，打發休閒時間！

* **`/blackjack`**：開始一場緊張刺激的 **21 點紙牌遊戲**，與莊家對決。
* **`/guess`**：1-100 猜數字遊戲，機器人會引導你猜出謎底。
* **`/trivia`**：進行益智問答，測驗您的常識庫！
* **`/slots`**：拉霸老虎機遊戲，試試您的手氣。
* **`/dice`**：擲出一般的 6 面骰子。
* **`/roll [公式]`**：擲骰子，支援進階 RPG 骰子公式（例如 `2d10+5` 擲兩顆 10 面骰加 5，或 `3d6` 擲三顆 6 面骰）。
* **`/rps [剪刀/石頭/布]`**：與機器人玩剪刀石頭布。
* **`/coinflip`**：翻轉硬幣（正面或反面）。
* **`/8ball [問題]`**：向神奇八號球提問，獲取神祕的預言指引。
* **`/joke`**：讓機器人說一個隨機笑話。
* **`/quote`**：隨機發送一句勵志的經典語錄。
* **`/choose [選項1] [選項2] ...`**：當您有選擇障礙時，讓機器人隨機幫您做決定。
* **`/emoji_rain [表情符號]`**：在聊天室裡下一場絢麗的表情符號雨。

---

## 💰 經濟與兌換碼系統

* **`/balance`**：查詢自己當前的金幣餘額。
* **`/daily`**：每日簽到領取隨機金幣。
* **`/pay [成員] [金額]`**：轉帳金幣給伺服器內的其他成員。
* **`/leaderboard`**：查看目前伺服器內的富豪金幣排行榜。
* **`/work`**：打工賺取金幣（每 30 分鐘可執行一次）。
* **`/redeem_code [兌換碼]`**：輸入活動兌換碼，領取金幣或專屬道具獎勵。
* **`/add_code [兌換碼] [獎勵金幣] [使用限制次數]`** (管理員/擁有者)：新增一組活動兌換碼。
* **`/reset_code [兌換碼]`** (管理員/擁有者)：重置該兌換碼的使用狀態。

---

## 🛠️ 實用工具與 AI 指令

* **`/translate [文字] [目標語言]`**：自動辨識文字來源語言，並將其翻譯為目標語言（例如繁體中文、英文、日文）。
* **`/weather [城市]`**：查詢指定城市（如台北、東京、紐約）的即時天氣與氣溫狀況。
* **`/qr [文字或連結]`**：將網址或文字一鍵轉換成二維條碼 (QR Code) 圖片。
* **`/shorten [網址]`**：將冗長的網址縮短為 TinyURL 短網址。
* **`/msg_where [關鍵字]`**：在頻道歷史紀錄中搜尋包含指定關鍵字的對話。
* **`/yt [關鍵字]`**：在頻道中快速搜尋 YouTube 影片並取得連結與播放控制。
* **`/bugreport [問題描述]`**：發現機器人有問題？一鍵回報 Bug 給開發人員。
* **`/server_information`**：將伺服器的完整結構（包含所有頻道、身分組、成員名冊）安全地打包為 ZIP 檔案下載，方便備份。
* **`/google_search [查詢內容]`**：直接在 Discord 聊天欄中呈現 Google 的搜尋結果摘要。
* **`/google_ai [聊天內容]`**：快速跳轉到 Phind 進行 AI 開發者對答。
* **`/google_image [提示詞]`**：快速跳轉到 Lexica.art 生成 AI 圖庫。
* **`/analyze_code [程式碼片段]`**：分析並識別該段程式碼是用什麼程式語言（Python、JS、C++ 等）編寫的。
* **`/font [文字]`**：將英文字轉換成 24 種特別的藝術英文字型（如粗體、手寫體、泡泡字）。

---

## 📅 系統、工單與其他功能

* **`/vote [問題] [選項1] [選項2] ...`**：快速在頻道中建立一個帶有反應按鈕的投票箱。
* **`/countdown [時間描述]`**：設定一個倒數計時器（例如 `10m`、`1h`），時間到時機器人會提及您並提醒。
* **`/reminder [時間描述] [提醒內容]`**：設定待辦事項提醒。
* **`/ticket`** (管理員限制)：在頻道部署「📩 開啟工單」面板，成員點擊後會自動建立私人的一對一客服諮詢頻道。
* **`/create_pirate_channel`**：建立一個帶有特殊屬性的專屬「海賊王」討論頻道。
* **`/youtube [新增/刪除] [YouTube 頻道ID] [通知文字]`**：
  * 設定 YouTube 新片自動推播通知。當該頻道有新影片上傳時，機器人會立即在指定頻道發送通知與連結。
* **`/exam-deploy`**：在指定頻道部署考試入口面板，允許成員進行學科測驗或認證。
* **`/sponsor`**：查看金主排行榜與贊助資訊。
* **`/promo [內容]`**： Bot 擁有者設定全域的輪替宣傳狀態。
* **`/help`**：叫出官方互動式指令分類選單。
* **`/ping`**：測試機器人與 Discord 伺服器之間的即時延遲（Ping 值）。
* **`/botinfo`**：查看機器人的開發資訊、所用庫、記憶體使用率與運作狀態。
* **`/serverinfo`** / **`/userinfo`** / **`/avatar`**：快速查詢伺服器、個人/他人帳號詳細資訊與高解析度頭像。
* **`/uptime`** / **`/version`** / **`/about`** / **`/invite`** / **`/join`**：查詢機器人的運行天數、當前版本日誌、開發背景、官方邀請連結並加入社群。
